{% extends 'base.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}
<div class="container">

    <h3 class="text-center">Modifier le type de véhicule</h3>
<div class=" d-flex justify-content-sm-center ">

    {{ include('type/_form.html.twig', {'button_label': 'Modifier'}) }}
</div>
<div class="text-center">
    {{ include('type/_delete_form.html.twig') }}
    <a href="{{ path('app_type_index') }}" class="btn btn-info">Retour</a>

    </div>
</div>

{% endblock %}
