{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
    <h1>Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ type.marque }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ type.model }}</td>
            </tr>
            <tr>
                <th>Puissance</th>
                <td>{{ type.puissance }}</td>
            </tr>
            <tr>
                <th>Carburant</th>
                <td>{{ type.carburant }}</td>
            </tr>
            <tr>
                <th>Boite_vitesse</th>
                <td>{{ type.boiteVitesse }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ type.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_index') }}">back to list</a>

    <a href="{{ path('app_type_edit', {'id': type.id}) }}">edit</a>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
