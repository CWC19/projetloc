{% extends 'base.html.twig' %}

{% block title %}Edit Voiture{% endblock %}

{% block body %}
<div class="container">

    <h1 class="text-center">Edit Voiture</h1>
    <div class="col-md-6 taille">    
        {{ include('voiture/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_voiture_index') }}">back to list</a>

        {{ include('voiture/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
